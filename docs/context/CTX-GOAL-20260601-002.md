# GOAL-20260601-002 上下文 Context

## 目标范围

本上下文记录 `docs/goal.md` 对 L0 kernel v1.0 的交付闭环要求。范围限定为 `github.com/ZoneCNH/kernel` 内的 xlib-standard 基础包、契约文档、发布证据和 CI 门禁。

## 当前边界

- 模块路径：`github.com/ZoneCNH/kernel`。
- 发布版本：`v0.1.0`。
- 代码边界：`errx`、`timex`、`lifecycx`、`retryx`、`healthx`、`obsx`、`validx`、`syncx`、`versionx`、`contracttest`。
- 文档边界：规范、设计、ADR、发布证据、复盘和评审均以本仓库内的 markdown 工件为准。

## 证据约束

完成证据必须至少覆盖 `go test ./...`、`go test -race ./...`、`go vet ./...`、`make docs-check`、`make boundary-check`、`make evidence-check` 和 `make release-preflight VERSION=v0.1.0`。
