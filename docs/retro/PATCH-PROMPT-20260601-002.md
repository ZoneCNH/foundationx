# PATCH-PROMPT-20260601-002 提示输出 Prompt Output

## 输入约束 Input Constraints

本轮修正由 `docs/goal.md` 的 Required Deliverables 驱动，重点补齐精确命名工件、包级文档示例、consumer 烟测证据和发布门禁覆盖。

## 执行提示 Execution Prompt

修正时必须保持 L0 依赖边界，不引入新依赖，不修改 x.go 仓库，并通过本地 release preflight 证明交付闭合。

## 输出要求 Output Requirements

所有新增工件必须由 `make docs-check`、`make release-evidence-check` 或 `make release-preflight VERSION=v0.1.0` 可验证。
