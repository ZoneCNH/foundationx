# PATCH-RULE-20260601-002 规则输出 Rule Output

## 防回归规则 Regression Rule

`scripts/ci/artifact-check.sh` 和 `scripts/check_release_evidence.sh` 必须显式列出 `docs/goal.md` 要求的上下文、spec、design、ADR、review、retro、PATCH、consumer smoke、package README、package example 和 golden 示例工件。

## 修改规则 Change Rule

新增或重命名必交付工件时，必须同步更新文档检查、发布证据检查和 release evidence 文档。

## 边界规则 Boundary Rule

PATCH 修复不得通过新增外部依赖、隐藏全局状态或外部系统连接来满足目标。
