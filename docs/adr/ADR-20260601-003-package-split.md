# ADR-20260601-003 包拆分 Package Split

## 状态

Accepted

## 背景

L0 kernel 需要同时提供错误、重试、生命周期、健康、观测、校验、同步、版本和契约测试能力。

## 决策

按能力拆分为独立包，公共依赖仅允许指向标准库或同层最小辅助能力，禁止引入业务层依赖。

## 后果

调用方可以按需导入小包；新增能力必须先通过边界检查证明不会扩大基础层耦合。
