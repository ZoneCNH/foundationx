# ADR-20260601-006 观测脱敏 Observability Redaction

## 状态

Accepted

## 背景

基础库可能被日志和错误处理路径复用，秘密值必须默认安全。

## 决策

`obsx.SecretString` 在字符串格式化、清理和 JSON 输出中默认返回 `***`，只保留显式调用方可控的原始值访问能力。

## 后果

默认输出更安全；需要明文的场景必须在调用点显式选择并接受审计。
