version: "2"

run:
  timeout: 3m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

linters-settings:
  staticcheck:
    checks:
      - all
      - -ST1000
      - -ST1003

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
