# XGO 消费方最小导入 XGO minimal import

This package is a kernel-side consumer compatibility fixture. It proves downstream code can import stable public kernel packages with `GOWORK=off` and without third-party runtime dependencies.
